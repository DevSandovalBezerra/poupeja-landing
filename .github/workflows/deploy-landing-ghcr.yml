name: Build and Push Landing to GHCR

on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  docker:
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push landing image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: true
          build-args: |
            VITE_SUPABASE_URL=${{ secrets.VITE_SUPABASE_URL }}
            VITE_SUPABASE_ANON_KEY=${{ secrets.VITE_SUPABASE_ANON_KEY }}
            VITE_LOG_LEVEL=info
            VITE_USE_REDIS=${{ secrets.VITE_USE_REDIS }}
            VITE_REDIS_URL=${{ secrets.VITE_REDIS_URL }}
            VITE_REDIS_TOKEN=${{ secrets.VITE_REDIS_TOKEN }}
          tags: |
            ghcr.io/devsandovalbezerra/poupeja-landing:latest
            ghcr.io/devsandovalbezerra/poupeja-landing:${{ github.sha }}
